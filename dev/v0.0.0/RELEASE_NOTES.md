# ESP_WIFI_MQTT v0.0.0 (아카이브 · 과거 v1.0.0)

## 주요 변경

- STA에 인터넷이 있을 때 설정 페이지에서 GitHub OTA(확인/설치/상태) 지원
- mbedtls 힙 OOM 방지를 위해 SoftAP HTTPS는 비활성화

빌드: Visol_WIFI_Module_V_ESP @ e6fd640

## 아카이브 안내 (2026-08-06)
- 과거 공개 버전 **v1.0.0** → **v0.0.0**로 폴더명을 바꾸고 `dev/`로 이동했습니다 (프로덕션 1.0.0 리베이스).
- 파일명·텍스트 manifest는 `v0.0.0` 기준으로 정리됐을 수 있으나, 바이너리 내부 버전 문자열은 원래 빌드값(예: `1.0.0`)을 유지합니다(재빌드 없음).
- 아카이브용으로 고친 `manifest.json`의 서명/`manifest.sig`는 검증이 실패할 수 있습니다. 프로덕션 OTA 최신 탐색 대상이 아닙니다.
